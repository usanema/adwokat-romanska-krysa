---
import Button from '@components/ui/Button.astro';
---

<section class="relative min-h-[90vh] flex items-center bg-gradient-to-br from-white via-gray-50 to-gray-100">
  <div class="container mx-auto">
    <div class="max-w-4xl mx-auto text-center py-20">
      <!-- Main Heading -->
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-heading font-semibold text-gray-950 mb-6 leading-tight">
        Profesjonalna pomoc prawna,<br />
        <span class="text-burgundy">której możesz zaufać</span>
      </h1>

      <!-- Subtitle -->
      <p class="text-xl md:text-2xl text-gray-600 mb-12 leading-relaxed max-w-2xl mx-auto">
        Kompleksowa obsługa prawna dla osób prywatnych i firm. Ponad 15 lat doświadczenia.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <Button variant="primary" size="lg" href="/specjalizacje">
          Nasze Specjalizacje
        </Button>
        <Button variant="outline" size="lg" href="/kontakt">
          Skontaktuj się
        </Button>
      </div>

      <!-- Trust Indicators -->
      <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-8 max-w-3xl mx-auto">
        <div class="text-center">
          <div class="text-4xl font-heading font-semibold text-burgundy mb-2">15+</div>
          <div class="text-sm text-gray-600">Lat doświadczenia</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-heading font-semibold text-burgundy mb-2">500+</div>
          <div class="text-sm text-gray-600">Wygranych spraw</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-heading font-semibold text-burgundy mb-2">200+</div>
          <div class="text-sm text-gray-600">Zadowolonych klientów</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-heading font-semibold text-burgundy mb-2">24/7</div>
          <div class="text-sm text-gray-600">Dostępność</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>
