---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export async function getStaticPaths() {
  const specializations = [
    {
      slug: 'prawo-karne',
      title: 'Obrona w sprawach karnych',
      subtitle: 'Katowice i Śląsk',
      lead: 'Zapewniam natychmiastową pomoc prawną w przypadku zatrzymania lub postawienia zarzutów. Działam osobiście, 24/7.',
      scope: [
        'Obrona w postępowaniu przygotowawczym (Policja)',
        'Reprezentacja przed Sądem',
        'Sprawy narkotykowe',
        'Jazda pod wpływem alkoholu',
        'Wnioski o dozór elektroniczny',
        'Uchylenie aresztu'
      ],
      icon: '⚖️'
    },
    {
      slug: 'prawo-medyczne',
      title: 'Prawo Medyczne i Błędy w Sztuce',
      subtitle: 'Pomoc dla pacjentów i lekarzy',
      lead: 'Specjalizuję się w dochodzeniu roszczeń za błędy medyczne oraz w obronie personelu medycznego.',
      scope: [
        'Błędy diagnostyczne i operacyjne',
        'Zakażenia szpitalne',
        'Naruszenie praw pacjenta',
        'Odszkodowania i zadośćuczynienia',
        'Obrona lekarzy w postępowaniu dyscyplinarnym'
      ],
      icon: '⚕️'
    },
    {
      slug: 'prawo-rodzinne',
      title: 'Rozwody i Prawo Rodzinne',
      subtitle: 'Dyskrecja i Skuteczność',
      lead: 'Przeprowadzę Cię przez rozwód z minimalnym stresem, dbając o Twój majątek i dobro dzieci.',
      scope: [
        'Rozwody (z orzekaniem o winie i bez)',
        'Podział majątku wspólnego',
        'Alimenty (ustalenie, podwyższenie, obniżenie)',
        'Opieka nad dziećmi i kontakty',
        'Ustalenie ojcostwa'
      ],
      icon: '👨‍👩‍👧'
    },
    {
      slug: 'obsluga-firm',
      title: 'Obsługa Prawna Firm',
      subtitle: 'Bezpieczeństwo Twojego Biznesu',
      lead: 'Kompleksowe wsparcie prawne dla przedsiębiorców i podmiotów leczniczych.',
      scope: [
        'Stała obsługa prawna',
        'Opiniowanie i tworzenie umów',
        'Windykacja należności',
        'Spory pracownicze i prawo pracy',
        'Reprezentacja w sporach sądowych'
      ],
      icon: '🏢'
    }
  ];

  return specializations.map(spec => ({
    params: { slug: spec.slug },
    props: { spec },
  }));
}

const { spec } = Astro.props;
---

<Layout title={`${spec.title} | Adwokat Anna Romańska-Krysa`}>
  <Header />
  
  <main class="spec-page">
    <div class="container grid-sidebar">
      
      <!-- LEWA KOLUMNA: TREŚĆ -->
      <article class="content">
        <header class="content-header">
           <span class="icon-big">{spec.icon}</span>
           <h1>{spec.title}</h1>
           <p class="subtitle">{spec.subtitle}</p>
        </header>

        <p class="lead-text">{spec.lead}</p>

        <hr class="divider" />

        <section class="scope-section">
          <h2>Zakres pomocy</h2>
          <ul class="scope-list">
            {spec.scope.map(item => (
              <li>{item}</li>
            ))}
          </ul>
        </section>

        <hr class="divider" />

        <section class="why-me">
           <h3>Dlaczego ja?</h3>
           <div class="why-grid">
             <div class="why-item">
               <span class="why-icon">🛡️</span>
               <h4>Bezpieczeństwo</h4>
               <p>Pełna dyskrecja i tajemnica adwokacka.</p>
             </div>
             <div class="why-item">
               <span class="why-icon">⏱️</span>
               <h4>Szybkość</h4>
               <p>Niezwłoczne podjęcie działań w sprawie.</p>
             </div>
             <div class="why-item">
               <span class="why-icon">🤝</span>
               <h4>Jasne zasady</h4>
               <p>Przejrzyste warunki finansowe od początku.</p>
             </div>
           </div>
        </section>

        <section class="faq-section">
          <h3>Częste pytania (FAQ)</h3>
          <details>
            <summary>Czy muszę przyjść osobiście?</summary>
            <p>Wiele spraw możemy omówić online lub telefonicznie, jednak w sprawach karnych i rodzinnych zalecam przynajmniej jedno spotkanie osobiste.</p>
          </details>
          <details>
            <summary>Ile kosztuje porada prawna?</summary>
            <p>Koszt ustalamy indywidualnie w zależności od skomplikowania sprawy. Zadzwoń, aby poznać wstępną wycenę.</p>
          </details>
        </section>
      </article>

      <!-- PRAWA KOLUMNA: STICKY SIDEBAR -->
      <aside class="sidebar sticky-sidebar">
        <div class="contact-box">
           <h3>Potrzebujesz pomocy?</h3>
           <p>Twoja sprawa nie może czekać? Skontaktuj się ze mną.</p>
           
           <a href="tel:+48123456789" class="sidebar-phone">
             📞 123 456 789
           </a>
           
           <a href="/kontakt" class="btn btn-primary full-width">Napisz E-mail</a>
           
           <div class="address-box">
             <strong>Kancelaria Adwokacka</strong><br>
             ul. Przykładowa 1<br>
             40-000 Katowice
           </div>
        </div>
      </aside>

    </div>
  </main>
  
  <Footer />
</Layout>

<style>
  .spec-page {
    padding: var(--spacing-xl) 0;
  }
  
  /* Content Styling */
  .icon-big {
    font-size: 3rem;
    display: block;
    margin-bottom: var(--spacing-sm);
  }
  
  .subtitle {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: var(--color-text-light);
    margin-top: -0.5rem;
  }
  
  .lead-text {
    font-size: 1.25rem;
    font-weight: 500;
    margin: var(--spacing-lg) 0;
    padding-left: var(--spacing-md);
    border-left: 4px solid var(--color-accent);
  }
  
  .divider {
    border: 0;
    border-top: 1px solid rgba(0,0,0,0.1);
    margin: var(--spacing-xl) 0;
  }
  
  .scope-list {
    list-style: none;
    padding: 0;
  }
  
  .scope-list li {
    background-color: var(--color-white);
    padding: 1rem;
    margin-bottom: 0.5rem;
    border-left: 3px solid var(--color-primary);
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-md);
    margin-top: var(--spacing-md);
  }
  
  .why-item {
    text-align: center;
  }
  
  .why-icon {
    font-size: 2rem;
    display: block;
    margin-bottom: 0.5rem;
  }
  
  .why-item h4 {
    font-size: 1.1rem;
    margin-bottom: 0.25rem;
  }
  
  .why-item p {
    font-size: 0.9rem;
  }

  /* FAQ */
  details {
    background: var(--color-white);
    margin-bottom: 0.5rem;
    padding: 1rem;
    cursor: pointer;
  }
  
  summary {
    font-weight: 600;
  }
  
  details p {
    margin-top: 0.5rem;
    margin-bottom: 0;
    color: var(--color-text-light);
  }

  /* Sidebar */
  .contact-box {
    background-color: var(--color-primary);
    color: var(--color-white);
    padding: 2rem;
    border-radius: 2px;
    text-align: center;
  }
  
  .contact-box h3 {
    color: var(--color-accent);
    border-bottom: 1px solid rgba(255,255,255,0.1);
    padding-bottom: 1rem;
    margin-bottom: 1rem;
  }
  
  .contact-box p {
    color: rgba(255,255,255,0.8);
    font-size: 0.95rem;
  }
  
  .sidebar-phone {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
    margin: 1.5rem 0;
  }
  
  .full-width {
    width: 100%;
    margin-bottom: 1.5rem;
    background-color: var(--color-accent);
    color: var(--color-primary);
    border-color: var(--color-accent);
  }
  
  .full-width:hover {
    background-color: var(--color-white);
    color: var(--color-primary);
  }
  
  .address-box {
    font-size: 0.9rem;
    color: rgba(255,255,255,0.7);
    text-align: left;
    margin-top: 1rem;
    border-top: 1px solid rgba(255,255,255,0.1);
    padding-top: 1rem;
  }

  @media(max-width: 768px) {
    .why-grid { grid-template-columns: 1fr; }
  }
</style>
