---
import Layout from "../layouts/Layout.astro";
import Button from "../components/Button";
import { specializations } from "../data";
import ServiceDetail from "../components/ServiceDetail";
---

<Layout
    title="Specjalizacje | Adwokat Anna Romańska-Krysa"
    activeTab="services"
>
    <div class="pt-24 pb-20 bg-cream animate-fade-in">
        <div class="container mx-auto px-6">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h1 class="text-4xl md:text-5xl font-serif text-green mb-6">
                    Specjalizacje
                </h1>
                <p class="text-lg text-text/70 font-sans">
                    Koncentruję się na konkretnych dziedzinach prawa, aby
                    zapewnić merytoryczne wsparcie na najwyższym poziomie.
                </p>
            </div>

            <div class="space-y-20">
                {
                    specializations.map((spec, index) => (
                        <ServiceDetail spec={spec} index={index} />
                    ))
                }
            </div>

            <div class="mt-20 p-8 bg-green text-cream text-center rounded-sm">
                <h3 class="text-2xl font-serif mb-4">
                    Twoja sprawa nie wpisuje się w powyższe ramy?
                </h3>
                <p class="mb-8 font-sans opacity-80">
                    Skontaktuj się ze mną. Jeśli nie będę w stanie pomóc, polecę
                    zaufanego specjalistę z innej dziedziny.
                </p>
                <Button variant="gold" href="tel:+48123456789"
                    >Zadzwoń: +48 123 456 789</Button
                >
            </div>
        </div>
    </div>
</Layout>
